<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

# Reglas de IA y Modelos (SIGPDA-EMS)

## Modelos autorizados
- **Modelo estándar y premium por defecto**: `gemini-3.5-flash-lite` (proveedor `gemini`). Definidos en `src/lib/ai-provider/index.ts` (`DEFAULT_STANDARD_MODEL`, `DEFAULT_PREMIUM_MODEL`).
- Los scripts offline (ej. `scripts/seed-*.js`, `scripts/ingest-normateca.js`) usan exclusivamente Gemini (vía `GEMINI_API_KEY` de `.env.local` o la tabla `api_keys` con `provider='gemini'`).
- **NO agregar** nuevos proveedores, claves externas ni modelos hardcodeados sin aprobación del usuario. La cadena de fallback multi-proveedor de `src/lib/ai-provider/` existe solo como respaldo operativo; no se amplía por iniciativa propia.

## Reglas de orquestación
- Todo llamado de IA en la app debe pasar por `src/lib/ai-provider` (`generateWithRotation` / `generateStreamWithRotation`), nunca llamar SDKs externos directamente desde rutas API o componentes.
- Los scripts offline que llamen IA deben reutilizar las claves del proyecto (env o tabla `api_keys`), nunca introducir claves nuevas ni pedirlas al usuario.

## Normateca SEP (tablas `normativa_documentos` / `normativa_articulos`)
- `scripts/ingest-normateca.js` procesa SOLO las carpetas de `documentos_referencia\[08] Normateca` listadas en `ALLOWED_FOLDERS`.
- Reglas de extracción obligatorias (verificables en el prompt del script):
  1. El texto de cada artículo es transcripción **LITERAL** del documento, nunca resumen ni parafraseo.
  2. Los documentos sin relevancia educativa (fiscal, hacendaria, administrativa no escolar, etc.) se marcan `aplica: false` y NO se insertan.
  3. `aplicable_a` se clasifica por artículo entre `['pmc','paec','pips','planeacion']`; no se asigna a todos por defecto.
- Los documentos irrelevantes o duplicados se **desactivan** con `vigente=false` (nunca se borran); `getNormativaForGenerator` (`src/lib/normativa-context.ts`) solo inyecta artículos de documentos `vigente=true`.
- **Alcance de inyección (decisión del usuario, 2026-08-08)**: la normativa se inyecta ÚNICAMENTE en **PMC** (`/api/pmc/[id]/generate-step/route.ts`) y **PIPS** (`/api/pips/[id]/generate/route.ts`). PAEC-PEC y Planeaciones NO inyectan normativa (los documentos reales de la Zona 004 no citan leyes; el formato DBEPA de planeación no la incluye). Los artículos clasificados como `paec`/`planeacion` permanecen en la BD para uso futuro.

## Arquitectura de Generación de Extras Didácticos (PDF / DOCX)
- **Motor Canónico y Orquestador Visual**: `src/lib/pdf-extra-renderer.ts` es el SSoT que implementa el maquetado visual, ribbons de momentos didácticos, cajas pedagógicas, `renderFormattedBlock` con word-wrap para negritas inline y exporta `renderExtraDocument(doc, extra, context)` y `generateExtraPdfDocument(extra, context)`. Garantiza la inicialización automática de fuentes editoriales oficiales (`areEditorialFontsLoaded`).
- **Fachada Pública**: `src/lib/pdf-extra-generator.ts` exporta `generateExtraPDF(extra, context)` para mantener total compatibilidad con las llamadas de consumo único individual (`/api/pdf/extra/[extraId]`).
- **Descargas Masivas (Bulk)**: Las rutas masivas (`/api/pdf/extra/bulk`) y pipelines de exportación empaquetada utilizan `generateExtraPdfDocument` para generar buffers optimizados empaquetados en archivos ZIP sin redundancias.
- **DOCX SSoT**: Los generadores Word de extras, secuencias y planeaciones delegan en `src/lib/docx-helpers.ts` (`buildExtraDocx`, `tbl`, `tc`, `tcH`, `bdr`, `parseTextRuns`, `createParagraphFromLine`) para consistencia normativa de estilos institucionales DBEPA Puebla.

## PMC Wizard — Ingesta Documental y Momentos Estadística 911 (Decisión N-001)
- **Momentos de Estadística Escolar 911**: El wizard PMC soporta formalmente los 3 cortes oficiales en `/api/pmc/estadistica-911`:
  1. `fin_anterior` (911 fin de ciclo previo): Alimenta indicadores de cierre (abandono escolar, eficiencia terminal, reprobación y aprobación).
  2. `inicio_actual` (911 inicio ciclo vigente): Alimenta matrícula vigente activa y total de docentes.
  3. `inicio_anterior` (911 inicio ciclo previo): Alimenta matrícula inicial de referencia histórica para trazabilidad.
- **Acceso en UI (Decisión N-001)**: El momento `inicio_anterior` cuenta con botones dedicados de carga en Paso 1 (Datos del Plantel) y Paso 3 (Diagnóstico Institucional) para planteles que disponen de dicho corte histórico, integrándose armónicamente con los flujos de `fin_anterior` e `inicio_actual`.
- **Badges de Estado (`docsStatus`)**: Los botones de subida (F11, 911 y PMC anterior) proporcionan retroalimentación visual (`✓`) al completarse la ingesta, previniendo cargas redundantes en la sesión del wizard.

## Plataforma Nivel 2 — Feature Flags y Patrón Strangler Fig
- **Servicio Canónico**: `src/lib/platform/feature-flags.ts` provee `FeatureFlagService` y los helpers `isFeatureEnabled`, `setFeatureFlag`, `resetFeatureFlags`.
- **Restricción Server-Side Only**: Las feature flags operan exclusivamente en Node.js runtime / server-side (`process.env`). NO utilizan el prefijo `NEXT_PUBLIC_` para evitar exponer banderas de infraestructura o lógica interna al bundle cliente del navegador.
- **Valores Predeterminados Seguros**: Todas las banderas de subsistemas y orquestadores (`PMC_ORCHESTRATOR_V2`, `PLANEACION_ORCHESTRATOR_V2`, `PAEC_ORCHESTRATOR_V2`, `CARTOGRAFIA_ORCHESTRATOR_V2`, `HORARIOS_ORCHESTRATOR_V2`) tienen valor predeterminado `false`.
- **Patrón Strangler Fig**: La migración hacia los Orquestadores Centrales Nivel 1 se realiza ruta por ruta. Cuando la bandera está en `false`, la ruta ejecuta el flujo legacy probado e intacto. Cuando la bandera está en `true`, la ruta delega en el orquestador correspondiente sin alterar los contratos JSON ni los códigos de respuesta HTTP (`200`, `400`, `404`, `422`).
- **Política de Cero Stubs Silenciosos (B-001)**: Los orquestadores y proveedores N1 (`PmcOrchestrator`, `PlaneacionOrchestrator`, `PaecOrchestrator`, `CartografiaContextProvider`, `HorariosOrchestrator`) tienen estrictamente prohibido simular éxito con buffers vacíos (`Buffer.from('')`) o retornos ficticios en métodos no migrados. Cualquier método en desarrollo lanza `501 NOT_IMPLEMENTED` de forma ruidosa.
- **Sondeo Honesto de Salud (B-002 / C-001)**: Las implementaciones de `healthCheck()` verifican el estado real de la infraestructura (configuración de base de datos `databaseConfigured`, credenciales de IA `aiServiceConfigured` y servicio de banderas `featureFlagService`). Si falta alguna configuración crítica, el subsistema reporta `status: 'degraded'` con detalle booleano por verificación, eliminando sondeos tautológicos.
- **Resiliencia de Ingesta y Proveedores de IA (Incidente 503 / 504)**:
  - **Fallback Dinámico**: `DEFAULT_MODEL_BY_PROVIDER.openrouter` utiliza el enrutador inteligente `openrouter/free` (configurable vía `OPENROUTER_FALLBACK_MODEL`), garantizando conmutación inmediata a modelos disponibles.
  - **Fast-fail en Caídas Globales (503)**: El rotador de claves (`key-rotator.ts`) detecta errores 503 de alta demanda upstream; tras 2 claves consecutivas detiene la rotación del pool primario y delega en el proveedor alternativo, protegiendo las cuotas y reduciendo drásticamente la latencia.
  - **Mapeo de Códigos HTTP**: Las indisponibilidades de IA se responden con `HTTP 503` y mensaje empático para el usuario, evitando acusar falsamente al archivo (`HTTP 400`) o emitir errores internos (`HTTP 500`).
  - **Módulo Transversal de Resiliencia (`src/lib/ai-resilience.ts`)**: Centraliza `withTimeoutBudget`, `isUpstreamAIError` y `AI_OUTAGE_USER_MESSAGE`, desacoplando completamente los orquestadores N1 entre sí (evitando imports cruzados PAEC/Cartografía/Horarios → PMC). Soporta detección estricta ampliada contra caídas HTTP 503 crudas y Service Unavailable.
  - **Presupuesto de Tiempo Global Unificado por Request (D-001)**: Se calcula un único deadline global al inicio de cada solicitud (`const deadline = Date.now() + 90000;`). Las etapas secuenciales de OCR e IA consumen el presupuesto restante dinámicamente (`Math.max(1, deadline - Date.now())`). Esto garantiza de forma matemática que la suma acumulada de ejecución nunca exceda los 90s por request (dejando 30s de colchón frente al corte de 120s de Vercel y evitando 504 sucios). Activo en rutas legacy y orquestadores (`/api/pmc/f11`, `/api/pmc/estadistica-911`, `/api/pmc/parse-previous`, `/api/paec/parse-previous`).
  - **Credencial de IA de Línea Base en Entorno Vercel (D-003)**: El sondeo `healthCheck()` evalúa `aiServiceConfigured` mediante `Boolean(process.env.GEMINI_API_KEY)`. Para que los orquestadores reporten `status: 'healthy'`, los despliegues de producción en Vercel deben contar con la variable `GEMINI_API_KEY` definida en el dashboard como clave de línea base y fallback de salud, complementando el pool de rotación de la tabla `api_keys`.
- **Orquestador de Planeaciones (Fase C Piloto)**: `src/lib/planeaciones/orchestrator.ts` implementa `IProgramSystem` (`planeaciones`, `2.0.0`) orquestando la evaluación bajo el Anexo 12 USICAMM / Guía Laboral MCCEMS vía la bandera `PLANEACION_ORCHESTRATOR_V2`. La ruta piloto migrada es `/api/planeaciones/evaluar`. Los componentes de Reto Situado (`planning-evaluator.ts` 4/4) y renderizado PDF (`planning-pdf-renderer.ts`, `pdf-workbook-renderer.ts`) se mantienen desacoplados e intactos.
- **Orquestador de PAEC (Fase D Piloto)**: `src/lib/paec/orchestrator.ts` implementa `IProgramSystem` (`paec`, `2.0.0`) centralizando la ingesta y estructuración de proyectos previos comunitarios (MCCEMS / NEM) vía la bandera `PAEC_ORCHESTRATOR_V2`. La ruta piloto migrada es `/api/paec/parse-previous`. Los métodos no migrados (`generateProject`, `auditProject`, `renderDOCX`, `renderPDF`) lanzan `501 NOT_IMPLEMENTED`. Reto Situado (`planning-evaluator.ts`) se preserva criptográficamente intacto.
- **Cartografía Context Provider (Fase E Piloto)**: `src/lib/cartografia/context-provider.ts` implementa `IProgramSystem` (`cartografia`, `2.0.0`) centralizando la ingesta de la matriz de zona 911/F11 y la estructuración del contexto territorial vía la bandera `CARTOGRAFIA_ORCHESTRATOR_V2`. La ruta piloto migrada es `/api/pips/parse-excel`. Los métodos no migrados (`generateMomento`, `generatePedagogicalMemory`, `renderPDF`, `exportContextForPmc`) lanzan `501 NOT_IMPLEMENTED`. Preserva el flujo metodológico continuo: Cartografía de Zona → PMC → PAEC → Planeación Docente + Bitácora 50-20-30. Reto Situado (`planning-evaluator.ts`) se mantiene criptográficamente inalterado.
- **Orquestador de Horarios Escolares (Fase F Piloto)**: `src/lib/horarios/orchestrator.ts` implementa `IProgramSystem` (`horarios`, `2.0.0`) centralizando la resolución heurística de restricciones (CSP) y diagnóstico pedagógico asistido por IA vía la bandera `HORARIOS_ORCHESTRATOR_V2`. La ruta piloto migrada es `/api/schedules/[id]/optimize`. Los métodos no migrados (`generateFullSchedule`, `exportScheduleExcel`, `renderSchedulePdf`, `exportSchedulePdf`, `validateTeacherAvailability`) lanzan `501 NOT_IMPLEMENTED`. Cero imports cruzados program-to-program. Reto Situado (`planning-evaluator.ts`) se mantiene criptográficamente inalterado.
- **Plataforma Nivel 2 — Endpoint Agregador Central de Salud**: `GET /api/platform/health` (`src/app/api/platform/health/route.ts`) consolida la verificación de conectividad y latencia de base de datos (`sql\`SELECT 1 as ping\``) y los sondeos honestos `healthCheck()` de los 5 subsistemas centrales de Nivel 1 (Planeaciones, PAEC, PMC, Cartografía y Horarios) sin alterar `src/proxy.ts`. Devuelve `200` (`healthy` o `degraded`) o `503` (`unhealthy`), garantizando observabilidad transversal confiable para la gobernanza del sistema.

## Registro de Valor por Orquestador Nivel 1 (Gobernanza y Análisis de Activación — Fase D-02)

Para dar estricto cumplimiento a la gobernanza de plataforma y las reglas R1/R2/R3, se registra la auditoría de valor técnico y operativo por cada uno de los 5 orquestadores N1. Ninguna recomendación propone "activar" sin una métrica cuantificada y validada.

| Orquestador / Flag | Qué centraliza hoy | Gap de paridad pendiente | Beneficio medible potencial de activarlo | Recomendación Técnica Fundamentada |
| :--- | :--- | :--- | :--- | :--- |
| **PMC** (`PMC_ORCHESTRATOR_V2`) | Extracción y normalización de F11 (`extractF11`), Estadística 911 (`extractEstadistica911`) y PMC anterior (`extractPreviousProject`) con reintento correctivo y null-safety. Sondeo de salud `healthCheck` y métricas. | Generación de pasos (`generate()` -> 501), importación de contexto territorial (`importPaec()` -> 501), renderizado de informe Word (`renderDOCX()` -> 501). | Eliminación de ~180 líneas de código duplicado entre rutas de extracción; latencia idéntica (+0ms de overhead); observabilidad de salud estandarizada (`/api/platform/health`). | **Mantener en `false`**. Las rutas legacy ya consumen el núcleo de resiliencia (`correctiveRetry`) y cálculo SSoT con 100% de paridad sin riesgo de regresión en flujos aún no migrados. Solo evaluar activación cuando `generate()` cuente con suite E2E completa. |
| **PAEC** (`PAEC_ORCHESTRATOR_V2`) | Ingesta y estructuración de proyectos previos comunitarios (`ingestPrevious`) con presupuesto de 90s, retry correctivo y reparación de nulls. Sondeo de salud `healthCheck`. | Generación comunitaria (`generateProject()` -> 501), auditoría pedagógica NEM (`auditProject()` -> 501), renderizado Word (`renderDOCX()` -> 501), renderizado PDF (`renderPDF()` -> 501). | Deduplicación de 95 líneas de validación documental; centralización de errores de ingesta en `PaecOrchestratorError` con mapeo de códigos HTTP (`400`, `422`, `503`). | **Mantener en `false`**. No existe beneficio de latencia (delta medido: <2ms) y la cobertura actual solo abarca 1 de 5 operaciones del ciclo de vida del PAEC. No invertir en activación hasta que el flujo de generación comunitaria sea prioritario. |
| **Planeaciones** (`PLANEACION_ORCHESTRATOR_V2`) | Evaluación pedagógica situada (`evaluate`) bajo Anexo 12 USICAMM y Guía Laboral MCCEMS. Persistencia atómica de `evaluation_json`. Sondeo de salud `healthCheck`. | Generación de secuencias (`generateSequence()` -> no expuesto), optimización de recursos didácticos (`optimizeResources()` -> no expuesto). | Trazabilidad uniforme de evaluaciones docentes en telemetría; tipado estricto `IProgramSystem`. | **Mantener en `false`**. La ruta legacy `/api/planeaciones/evaluar` opera con paridad exacta y 0 diferencias de rendimiento. Activar la bandera no reduce latencia ni aporta métricas adicionales a producción. |
| **Cartografía** (`CARTOGRAFIA_ORCHESTRATOR_V2`) | Ingesta de matriz de zona 911/F11 y contexto territorial (`ingestZoneMatrix`) con vinculación condicional a PAEC de BD. Sondeo de salud `healthCheck`. | Generación de Momento 3, 4, 5 (`generateMomento()` -> 501), memoria pedagógica (`generatePedagogicalMemory()` -> 501), renderizado de cartografía territorial (`renderPDF()` -> 501), exportación a PMC (`exportContextForPmc()` -> 501). | Unificación del contrato territorial para sincronización con supervisores escolares; tipado `CartografiaZoneMatrixIngestResult`. | **Mantener en `false`**. Solo migra 1 de 5 momentos de la cartografía. El flujo principal de generación de momentos didácticos continúa en rutas dedicadas. No invertir más esfuerzo hasta completar el ciclo territorial. |
| **Horarios** (`HORARIOS_ORCHESTRATOR_V2`) | Optimización de horarios (`optimizeSchedule`), resolución CSP (`reSolve`) y feedback pedagógico con IA (`aiFeedback`). Registro en `ai_optimization_log`. Sondeo de salud `healthCheck`. | Generación completa de horarios (`generateFullSchedule()` -> 501), validación de disponibilidad docente (`validateTeacherAvailability()` -> 501), exportación Excel (`exportScheduleExcel()` -> 501), renderizado PDF (`renderSchedulePdf()` -> 501). | Aislamiento de excepciones CSP en `HorariosOrchestratorError`; histórico estructurado de optimizaciones en el horario escolar. | **Mantener en `false`**. No presenta mejora medible en tiempo de resolución CSP (el motor `resolverHorario` es el mismo en ambos caminos). No justificado activar sin benchmark de reducción de huecos docentes en planteles reales. |

### Criterio de Gobernanza de Banderas N1 (Cierre de Fase D):
1. **Todas las banderas N1 permanecen en `false` por defecto**: La plataforma opera sobre las rutas legacy reforzadas con el núcleo compartido (`numeric-guard`, `ai-resilience`, `coverage-core`, `pmc-indicator-calculator`).
2. **Cero activación sin métrica verificada**: Cualquier propuesta futura de activación requerirá: (a) prueba de carga comparativa de latencia (p95/p99); (b) paridad funcional al 100% sin gaps 501 en las rutas del programa; y (c) aprobación explícita y documentada del usuario.

